# care-giver-api

## Prerequisites

- `Node` version 18
- `Yarn`

## Installation

- Clone the projet
- `./scripts/reset-db` to setup DB
- `./scripts/dev` to run development server

## Tests

- `./scripts/test` to run tests